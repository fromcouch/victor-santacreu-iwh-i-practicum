doctype html
html(lang="en")
    head
        title= `${title}`
        link(rel="stylesheet", href="/css/style.css")
    body
        p 
            a(href=`/`) Return to the homepage
        .form-wrapper
            h1 Competition
            form(method="POST")
                label(for="newVal") Name
                input(type="text" name="name" required)
                label(for="newVal") Winner
                input(type="text" name="winner" required)
                label(for="newVal") Looser
                input(type="text" name="looser" required)
                input(type="submit" value="Submit")